# 3x-ui-ram-watchdog
```
wget -O /root/Install https://raw.githubusercontent.com/TspKchn/3x-ui-ram-watchdog/main/install-xui-watchdog-final.sh && chmod +x /root/Install && /root/Install && rm -f /root/Install

```
